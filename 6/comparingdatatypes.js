// comparing data types
let x = 5;
let y = "5";
console.log(x == y); //true equal=> checks only the value
console.log(x === y); // false strict equal not only checks the value but also checks the data type
console.log(typeof y); // string
